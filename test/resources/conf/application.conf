# https://www.playframework.com/documentation/latest/Configuration

slick {
  dbs {
    default {
      profile = "slick.jdbc.PostgresProfile$"
      db {
        driver = "org.postgresql.Driver"
        url = "jdbc:postgresql://localhost:5432/quoteDBTest"
        user = "postgres"
        password = "0000"
      }
    }
  }
}
